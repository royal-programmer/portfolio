/** Per-region motion: slides this many px along exit direction (left/right/up/down) */
export const REGION_SLIDE_PX = 52;
/** Each block exits over this duration (sequential: one after another) */
export const REGION_EXIT_DURATION_S = 0.38;
/** Each block enters over this duration (sequential) */
export const REGION_ENTER_DURATION_S = 0.42;

/** Full-screen wash (backdrop already swapped at wash start) */
export const PERSONA_WASH_MS = 620;
